# Index Profiles

### default  
Indexes ledger + templates + runtime logs

### ledger-only  
Fastest indexing. Recalls temporal chains.

### deep-scan  
Recursively indexes all `.ms` and `.ms.md` files.
